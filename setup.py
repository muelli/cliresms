#!/usr/bin/env python
from distutils.core import setup

setup(name='cliresms',
      version='0.1',
      description='A script to send webtexts (SMS) from the command line for Irish carriers (three, O2, vodafone)',
      url='https://github.com/russelldavies/cliresms',
      license='Apache V2',
      scripts=['cliresms'],
      install_requires = [
      ],
      extras_require = {
      }
)
